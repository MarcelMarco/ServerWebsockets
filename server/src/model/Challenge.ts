export class Challenge {
    id: string;
    name: string;
    descr: string;
  
    constructor(id: string, name: string, descr: string) {
      this.id = id;
      this.name = name;
      this.descr = descr;
    }
  }